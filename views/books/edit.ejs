<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Book</title>
</head>

<body>
    <h1>Edit Book Info</h1>
    <form action="/books/<%=book._id%>?_method=PUT" method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" id="title" name="book[title]" value="<%= book.title %>">
        </div>
        <div>
            <label for="description">description</label>
            <input type="text" id="description" name="book[description]" value="<%= book.description %>">
        </div>
        <div>
            <label for="price">price</label>
            <input type="text" id="price" name="book[price]" value="<%= book.price %>">
        </div>
        <button type="submit">Edit Book</button>
    </form>
    <a href="/books/<%= book._id %>">Back to Book</a>
</body>

</html>